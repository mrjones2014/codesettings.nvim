{
  "Lua.addonManager.enable": true,
  // test both styles for nixd
  "nix.serverSettings": {
    "nixd": {
      "formatting.command": [
        "nixfmt"
      ]
    }
  },
  "nixd.options.nixos.expr": "test nixos expr"
}
